import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.axolotedeaccion.conwaysgameoflife',
  appName: "Conway's Game Of Life",
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
